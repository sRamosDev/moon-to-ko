🧪 [testing improvement description]
Add missing coverage for untested extractors and exporters.

🎯 What: The testing gap addressed
Tested the extraction of epubs (`EpubExporter`) and replacement rules (`ReplacementsExporter`) from `.mrpro` files, and the main application script behavior (`src/main.py`), including the behavior of `MrproExtractor` under corner cases.

📊 Coverage: What scenarios are now tested
*   Testing that epub files are correctly parsed from the `MrproExtractor` out to `.epub` files.
*   Testing that global replacement rules are generated correctly for KOReader out to Lua files.
*   Testing that book-specific replacement rules are accurately tracked.
*   Testing `src/main.py` flow: from zip to extraction steps.
*   Tested exception flows.

✨ Result: The improvement in test coverage
`src/epub_exporter.py` now reaches 100% code coverage.
`src/replacements_exporter.py` now reaches 100% code coverage.
`src/mrpro_extractor.py` coverage improved from 89% to 100%.
`src/main.py` coverage improved from 21% to 98%.
Total codebase test coverage improved significantly from 51% to 80% (which leaves only the `src/gui.py` out from testing).
